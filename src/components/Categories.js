import React from 'react';

class Categories extends React.Component{
    componentDidMount() {
        document.title="Categories"
    }
    render(){
        return(<div><header>Welcome to Categories Screen</header></div>);
    }
}

export default Categories;